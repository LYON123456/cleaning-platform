## Agent skills

### Issue tracker

Issues tracked as GitHub Issues via the `gh` CLI (PRs-as-requests: off). See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical roles (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at repo root, created lazily by /domain-modeling). See `docs/agents/domain.md`.
